version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      path: .
    - requirements: requirements/docs.txt

mkdocs:
  configuration: mkdocs.yml
